/*
Return a funciton that takes a list of valid Users and returns a function that
returns true if all of the supplied users exist in the original list of users.
*/
module.exports = function(goodUsers){
  //if submitted users in list of goodusers.  return true
  return function allUsersValid(submittedUsers){

  };

};
