#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../functional-javascript-workshop/functional-javascript.js" "$@"
  ret=$?
else 
  node  "$basedir/../functional-javascript-workshop/functional-javascript.js" "$@"
  ret=$?
fi
exit $ret
